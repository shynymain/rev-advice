const CORS={"Access-Control-Allow-Origin":"*","Access-Control-Allow-Methods":"POST,OPTIONS","Access-Control-Allow-Headers":"content-type"};
function json(x){return new Response(JSON.stringify(x),{headers:{"content-type":"application/json;charset=utf-8",...CORS}})}
export default{async fetch(req,env){if(req.method==='OPTIONS')return new Response(null,{headers:CORS});const url=new URL(req.url);if(!url.pathname.endsWith('/api/advice'))return json({ok:false,error:'use /api/advice'});if(req.method!=='POST')return json({ok:false,error:'POST only'});const data=await req.json();if(!env.AI)return json({ok:true,advice:'AI Binding未設定です。予想ロジック結果を優先してください。'});const res=await env.AI.run('@cf/meta/llama-3.2-3b-instruct',{prompt:`競馬予想の補助コメントを日本語で簡潔に返してください。\n${JSON.stringify(data)}`,max_tokens:500});return json({ok:true,advice:res.response||JSON.stringify(res)})}}
