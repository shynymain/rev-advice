const headers={"content-type":"application/json;charset=utf-8","access-control-allow-origin":"*","access-control-allow-methods":"GET,POST,OPTIONS","access-control-allow-headers":"content-type"};
export default{async fetch(req){if(req.method==='OPTIONS')return new Response('ok',{headers});const url=new URL(req.url);if(url.pathname!=='/api/advice')return Response.json({ok:false,error:'use /api/advice'},{headers});let body={};try{body=await req.json()}catch(e){}return Response.json({ok:true,comment:'5系中心。◎ライン発生源と隣接を優先。買い目はフロント側ロジックを正とする。',received:!!body.race},{headers});}};
