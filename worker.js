const headers={"content-type":"application/json; charset=utf-8","access-control-allow-origin":"*","access-control-allow-methods":"GET,POST,OPTIONS","access-control-allow-headers":"content-type"};
const json=(x,status=200)=>new Response(JSON.stringify(x,null,2),{status,headers});
export default{async fetch(request){const url=new URL(request.url);if(request.method==="OPTIONS")return json({ok:true});if(url.pathname!=="/api/advice")return json({ok:false,error:"use /api/advice",races:[]},404);let body={};try{if(request.method==="POST")body=await request.json()}catch{}const p=body.prediction||{};const axis=p.axis?`${p.axis.no} ${p.axis.name||""}`:"未設定";return json({ok:true,races:[],advice:`軸候補は ${axis}。5系接続・◎隣・中位人気帯を優先。買い候補でない場合は金額0円保存または見送り推奨。`})}};
