const cors = {"access-control-allow-origin":"*","access-control-allow-methods":"POST,OPTIONS","access-control-allow-headers":"content-type"};
function json(data){ return new Response(JSON.stringify(data,null,2),{headers:{"content-type":"application/json;charset=utf-8",...cors}}); }
export default { async fetch(req, env){ if(req.method==="OPTIONS") return new Response(null,{headers:cors}); if(req.method!=="POST") return json({ok:false,error:"POST only"}); const data=await req.json().catch(()=>({})); if(!env.AI){ const axis=data.axis||{}; return json({ok:true,advice:`AI Binding未設定。簡易提案: 軸 ${axis.no||"?"} ${axis.name||""} / 自信度 ${data.confidence||"?"} / ${data.skip?"見送り優先":"購入候補"}`}); } const prompt = `競馬予想AIとして、以下の予想データを評価してください。短く返答。\n${JSON.stringify(data)}`; const r=await env.AI.run("@cf/meta/llama-3.2-3b-instruct",{prompt,max_tokens:400}); return json({ok:true,advice:r.response||String(r)}); }};
